<% content_for :body do %>
  <ul id="add_pages">
    <% @add_pages.each do |add_page| %>
      <li>
        <%= link_to add_page.name, refinery.add_pages_add_page_path(add_page) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
